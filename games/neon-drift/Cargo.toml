[package]
name = "neon-drift"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
libm = "0.2"

[profile.release]
opt-level = "s"
lto = true

# Standalone workspace (not part of parent zx-showcase workspace)
[workspace]

